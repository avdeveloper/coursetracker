define(["knockout", "HomeHelper"], function (ko, HomeHelper) {
  return function HomeViewModel () {
    var

    // ~private properties
    self = this,
    helper = new HomeHelper(self);

    // ~constructor

  }; // HomeViewModel
}); // definition
